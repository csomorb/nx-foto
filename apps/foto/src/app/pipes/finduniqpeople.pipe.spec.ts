import { FinduniqpeoplePipe } from './finduniqpeople.pipe';

describe('FinduniqpeoplePipe', () => {
  it('create an instance', () => {
    const pipe = new FinduniqpeoplePipe();
    expect(pipe).toBeTruthy();
  });
});
