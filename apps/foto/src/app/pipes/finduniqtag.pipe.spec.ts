import { FinduniqtagPipe } from './finduniqtag.pipe';

describe('FinduniqtagPipe', () => {
  it('create an instance', () => {
    const pipe = new FinduniqtagPipe();
    expect(pipe).toBeTruthy();
  });
});
